# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
apiVersion: core.oam.dev/v1beta1
kind: ComponentDefinition
metadata:
  annotations:
    definition.oam.dev/description: Kubernetes cluster anywhere (AWS, Azure, Google Cloud, Civo, DigitalOcean, etc.)
  name: kubernetes
spec:
  schematic:
    cue:
      template: |
        output: {
        	apiVersion: "devopstoolkitseries.com/v1alpha1"
        	kind:       "ClusterClaim"
        	metadata: name: context.name
        	spec: {
        		compositionRef: name: parameter.cluster
        		id: context.name
        		parameters: {
        			minNodeCount: parameter.minNodeCount
        			nodeSize:     parameter.nodeSize
        			version:      parameter.version
        		}
        		writeConnectionSecretToRef: name: context.name
        	}
        }
        outputs: {}
        parameter: {
        	cluster:      *"cluster-aws" | string
        	minNodeCount: *3 | int
        	nodeSize:     *"medium" | string
        	version:      *"1.22" | string
        }
  workload:
    definition:
      apiVersion: devopstoolkitseries.com/v1alpha1
      kind: ClusterClaim

